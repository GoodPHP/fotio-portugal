import type { BlogPost } from '../types';

/**
 * Shorter guides, beside the pillar articles in `blog.ts`.
 *
 * Emptied in the conversion — the previous set was about French regions.
 */
export const BLOG_GUIDES: BlogPost[] = [];
