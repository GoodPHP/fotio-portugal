import type { City } from '../types';

/**
 * The places people travel to: Sintra, Algarve, Madeira, Açores, Douro,
 * Comporta. Three of these are not cities at all, which is what `City.kind`
 * is for.
 *
 * Emptied rather than translated — see the note in `cities-metro.ts`.
 */
export const DESTINATION_CITIES: City[] = [];
